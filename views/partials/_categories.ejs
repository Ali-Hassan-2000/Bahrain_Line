<nav>
    <ul>
        <% categories.forEach(category => { %>
            <li>
                <a href="/items/<%= category._id %>">
                    <div>
                        <%= category.CategoryName %>
                    </div>
                </a>
            </li>
        <% }); %>
    </ul>
</nav>